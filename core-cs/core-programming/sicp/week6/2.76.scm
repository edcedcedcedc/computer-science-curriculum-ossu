#lang simply-scheme
;if a lot of type then ddp
;because you have intelligent operations that and can extend types 
;if a lot of operations then message passing
;bacause you have intelligent data objects that can always extend operations 