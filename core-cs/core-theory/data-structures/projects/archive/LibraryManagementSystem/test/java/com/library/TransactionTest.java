package projects.LibraryManagementSystem.test.java.com.library;

/* import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*; */

public class TransactionTest {
    @Test
    public void testTransactionCreation() {
        // Member member = new Member("John Doe", "M001");
        // Book book = new Book("Java Programming", "John Smith", "123456789");
        // Transaction transaction = new Transaction(member, book, "borrow");

        // assertNotNull(transaction);
        // assertEquals(member, transaction.getMember());
        // assertEquals(book, transaction.getBook());
        // assertEquals("borrow", transaction.getType());
    }

    @Test
    public void testToString() {
        // Member member = new Member("John Doe", "M001");
        // Book book = new Book("Java Programming", "John Smith", "123456789");
        // Transaction transaction = new Transaction(member, book, "borrow");

        // String expected = "Transaction[Member: John Doe, Book: Java Programming,
        // Type: borrow, Date: " + transaction.getDate() + "]";
        // assertEquals(expected, transaction.toString());
    }
}
